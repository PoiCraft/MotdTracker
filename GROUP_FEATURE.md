# 服务器分组与玩家数据聚合功能

## 功能说明

现在支持将同一服务器的多个连接入口(不同地区节点)聚合显示!

### ✨ 新增特性

#### 1. 分组视图 (默认)
- 将同一服务器的多个入口聚合为一个卡片
- **总在线玩家数**: 自动汇总所有入口的玩家数量
- **最大玩家数**: 取所有入口中的最大值
- **平均延迟**: 计算所有入口的平均延迟
- **活跃入口数**: 显示多少个入口当前在线

#### 2. 单服务器视图
- 点击"切换到单服务器视图"查看每个入口的独立数据
- 保留原有的详细监控信息

### 📊 聚合数据说明

#### 玩家数据
对于同一组的多个服务器入口(指向同一服务器):
- **玩家数据来自任一在线的入口** (优先选择在线的入口)
- 由于多个入口连接到同一服务器,玩家数据是共享的
- 显示的玩家数就是服务器实际的玩家数,不会累加
  - 例如: 主线2人、南京2人、重庆2人 → 实际是同一个服务器的2个玩家

#### 延迟数据聚合
- **平均延迟 = 所有在线入口延迟的平均值**
  - 例如: (120ms + 100ms + 220ms + 472ms) / 4 = 228ms
  - 反映从不同入口连接的平均网络状况

#### 在线率统计
- 统计所有入口的总检查次数和在线次数
- 更准确反映服务器整体可用性

### 🎮 配置方法

在 `config.json` 中为每个服务器添加 `group` 字段:

```json
{
  "servers": [
    {
      "name": "PoiCraft 主线",
      "host": "play.poicraft.com",
      "port": 10006,
      "group": "PoiCraft"
    },
    {
      "name": "PoiCraft 南京",
      "host": "p2p.gxb.icu",
      "port": 10086,
      "group": "PoiCraft"
    }
  ]
}
```

相同 `group` 值的服务器会被聚合显示。

### 📈 图表说明

#### 分组视图的图表
1. **延迟趋势图**: 显示所有入口的平均延迟
2. **在线玩家趋势图**: 显示服务器的实际玩家数(取自任一在线入口)
3. **在线状态历史**: 只要有任一入口在线就显示在线
4. **连接入口详情**: 列表显示每个入口的实时状态

### 🔄 切换视图

- **分组视图**: 查看聚合后的总体数据(推荐)
- **单服务器视图**: 查看每个入口的独立详细信息

点击"切换到单服务器视图/分组视图"按钮即可切换。

### 📊 API 端点

新增的分组API:
- `GET /api/groups` - 获取所有服务器组
- `GET /api/group/<group_name>/history` - 获取组的聚合历史数据
- `GET /api/group/<group_name>/stats` - 获取组的统计信息

### 💡 使用场景

适用于:
- 同一游戏服务器的多个区域节点(CDN)
- 主服务器 + 备用服务器
- 不同运营商入口(电信/联通/移动)
- 国内外不同接入点

### 示例

假设有4个入口,某一时刻的数据:
```
主线: 在线, 2人/20人, 120ms
南京: 在线, 2人/20人, 100ms  
重庆: 在线, 2人/20人, 220ms
深圳: 在线, 2人/20人, 472ms
```

分组视图显示:
```
在线玩家: 2/20人 (取自任一在线入口)
平均延迟: 228ms (所有入口平均)
活跃入口: 4/4
```

**说明**: 由于4个入口连接到同一个Minecraft服务器,服务器实际只有2个玩家在线,而不是8个。不同入口只是提供了不同的网络路径。
