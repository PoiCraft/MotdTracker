# 07 管理面板："选项..."设置屏

Status: ready-for-agent
Blocked by: 01
Spec: ../spec.md

## 目标

`/admin` 实现为 MC "选项..."设置屏，承载全部管理功能（现存最大的页面，注意拆分子组件）。

## 范围

1. 节点管理：列表 + 新增/编辑/删除/启用开关（MC 对话框确认）
2. 服务器组管理：CRUD + 排序
3. Webhook 告警配置
4. 应用设置：轮询间隔等（读现有 `/api/admin/*` 能力）
5. 修改密码
6. 全部表单：react-hook-form + zod，MC 控件；危险操作二次确认（MC 风格对话框）

## 验收

- 浏览器冒烟：节点/组/webhook 全 CRUD 走通，改动后端数据正确
- 未登录访问 `/admin` 重定向登录页
- 双语词条完整
