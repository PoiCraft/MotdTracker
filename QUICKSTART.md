# 快速启动指南

## 启动步骤

1. **激活虚拟环境** (如果尚未激活):
```powershell
.\.venv\Scripts\Activate.ps1
```

2. **安装依赖** (首次运行):
```powershell
uv sync
```

3. **启动应用**:
```powershell
python main.py
```

4. **访问Web界面**:
打开浏览器访问: http://127.0.0.1:5000

## 功能说明

应用启动后会:
- 立即轮询一次所有服务器
- 每60秒自动轮询一次
- 将数据保存到 `minecraft_stats.db`
- 在5000端口启动Web服务器

## Web界面特性

- 📊 **实时状态** - 显示每个服务器的当前状态
- 📈 **延迟图表** - Chart.js绘制的实时延迟趋势
- 📅 **历史状态条** - 最近120次检查的可视化状态
- 📉 **统计信息** - 24小时在线率和平均延迟
- 🔄 **自动刷新** - 每60秒自动更新页面

## 停止应用

按 `Ctrl+C` 停止服务器

## 修改配置

编辑 `config.json` 文件来:
- 添加/删除服务器
- 修改轮询间隔
- 更改数据库路径

修改后需要重启应用。
