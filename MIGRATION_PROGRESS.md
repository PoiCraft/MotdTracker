# 数据迁移进度展示

## 功能特性

现在数据迁移过程包含完整的进度展示，提供清晰的视觉反馈：

### 1. 迁移前统计

```
======================================================================
开始数据迁移：SQLite → PostgreSQL
======================================================================

📊 统计数据量...

待迁移数据统计:
  • 服务器: 5 条
  • 状态日志: 12,345 条
  • 玩家会话: 678 条
  • 会话历史: 3,456 条
  • 总计: 16,484 条记录
```

### 2. 实时进度条

每个迁移阶段都有可视化进度条：

```
🔄 [1/4] 迁移服务器数据...
    [██████████████████████████████████████████████████] 100% (5/5)
✅ 服务器迁移完成: 5 条

🔄 [2/4] 迁移状态日志...
    [█████████████████████████████░░░░░░░░░░░░░░░░░░░░░] 58% (7,200/12,345)
```

### 3. 阶段性完成提示

每个阶段完成后显示清晰的状态：

```
✅ 状态日志迁移完成: 12,345 条

🔄 [3/4] 迁移玩家会话数据...
    [████████████████████████████████████████████████████] 100% (678/678)
✅ 玩家会话迁移完成: 678 条
```

### 4. 完整迁移报告

所有数据迁移完成后提供详细统计：

```
======================================================================
✅ 数据迁移完成！
======================================================================

迁移统计:
  • 服务器: 5 条
  • 状态日志: 12,345 条
  • 玩家会话: 678 条
  • 会话历史: 3,456 条
  • 总计: 16,484 条记录

💾 备份原数据库...
✅ 备份完成: minecraft_stats.db.migrated

======================================================================
```

## 进度条特性

### 视觉元素
- **进度条长度**: 50 字符宽
- **已完成部分**: █ (实心方块)
- **未完成部分**: ░ (空心方块)
- **百分比显示**: 实时计算 (0-100%)
- **计数显示**: 当前数量/总数量，使用千位分隔符

### 实时更新
- 使用 `\r` 和 `flush=True` 实现同行更新
- 避免屏幕滚动，保持界面整洁
- 批量处理时每 1000 条更新一次进度

### 示例效果

```python
def show_progress(current, total, prefix=""):
    """显示进度条"""
    if total == 0:
        return
    percent = min(100, int(current * 100 / total))
    bar_length = 50
    filled = int(bar_length * current / total)
    bar = '█' * filled + '░' * (bar_length - filled)
    print(f"\r{prefix}[{bar}] {percent}% ({current:,}/{total:,})", end='', flush=True)
```

## 使用场景

### 自动迁移
应用首次启动时如果检测到 PostgreSQL 配置：
```bash
python poller.py
```

### 手动迁移
使用专用迁移工具：
```bash
python migrate.py
```

两种方式都会显示相同的进度展示。

## 技术细节

### 批量处理优化
- **状态日志**: 每 1000 条一批，避免内存溢出
- **玩家会话**: 逐条插入，显示实时进度
- **会话历史**: 逐条插入，显示实时进度

### 防重复机制
- 迁移完成后创建 `.migrated` 备份文件
- 下次启动检测到备份文件则跳过迁移
- 避免数据重复插入

### 错误处理
- 单条记录失败不中断整体迁移
- 错误信息记录到日志（debug 级别）
- 继续处理剩余数据

## 性能参考

基于测试数据的迁移时间参考：

| 数据量 | 预估时间 |
|--------|---------|
| 1,000 条状态日志 | 约 2-3 秒 |
| 10,000 条状态日志 | 约 20-30 秒 |
| 100,000 条状态日志 | 约 3-5 分钟 |
| 1,000,000 条状态日志 | 约 30-50 分钟 |

*实际时间取决于网络延迟、PostgreSQL 性能和数据复杂度*

## 注意事项

1. **大数据迁移**: 状态日志较多时请耐心等待
2. **网络稳定**: 确保到 PostgreSQL 服务器的网络连接稳定
3. **磁盘空间**: 备份文件会占用额外空间
4. **终端支持**: 某些终端可能不支持 Unicode 字符，可改用 ASCII 字符

## 故障排查

### 进度条不显示
- 检查终端是否支持 `\r` 回车符
- 确认 Python 输出未被重定向
- 某些 IDE 控制台可能不支持实时刷新

### 进度卡住不动
- 检查 PostgreSQL 连接状态
- 查看日志确认是否有错误
- 检查数据库锁或死锁情况

### 中断后恢复
目前不支持断点续传，中断后需要：
1. 清理 PostgreSQL 中的部分数据
2. 重新运行完整迁移
3. 或使用 `fix_pgsql.py` 重置数据库后再迁移
